version: 0.1
releases:
  "1.0":
    status: deployed
    docs: ""
    profiles:
      - base
    spec:
      files:
        .env:
          type: dotenv
          vars:
            ENV:
              type: string
              default: dev
            PROJECT_NAME:
              type: string
              default: envers
            VERSION:
              type: string
              default: "1.0"
        .env.test:
          type: dotenv
          vars:
            ENV2:
              type: string
              default: dev
            PROJECT_NAME2:
              type: string
              default: envers
            VERSION2:
              type: string
              default: "1.0"
  "2.0":
    status: deployed
    docs: ""
    profiles:
      - base
    spec:
      files:
        .env:
          type: dotenv
          vars:
            ENV:
              type: string
              default: dev
            PROJECT_NAME:
              type: string
              default: envers
            VERSION:
              type: string
              default: "1.0"
            USE_CONTAINER:
              type: bool
              default: true
