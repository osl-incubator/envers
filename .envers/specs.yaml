version: 0.1
releases:
  '1.0':
    status: draft
    help: ''
    profiles:
    - base
    spec:
      files:
        .env:
          type: dotenv
          vars:
            ENV:
              type: string
              default: dev
              encrypted: false
            PROJECT_NAME:
              type: string
              default: envers
              encrypted: false
            VERSION:
              type: string
              default: '1.0'
              encrypted: false
